import { createTheme } from "./utils";

const darkTheme = createTheme({
  primary: "#212529",
  secondary: "#343A40",
  accent: "#6C757D",
  accentLight: "#ADB5BD",
  textBase: "white",
});

export default darkTheme;
