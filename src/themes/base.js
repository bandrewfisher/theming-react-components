import { createTheme } from "./utils";

const baseTheme = createTheme({
  primary: "#219ebc",
  secondary: "#B0D7FF",
  accent: "#a6d3a0",
  accentLight: "#D1FFD7",
  textBase: "#656565",
});

export default baseTheme;
